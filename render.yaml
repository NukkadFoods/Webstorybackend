services:
  - type: web
    name: webstory-backend
    runtime: docker
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
      - key: RENDER
        value: "true"
